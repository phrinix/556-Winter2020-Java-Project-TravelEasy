window.onload = function(){
	new JsDatePick({
		useMode:2,
		target:"adate",
		dateFormat:"%F %d, %Y"
	});
	
	new JsDatePick({
		useMode:2,
		target:"ddate",
		dateFormat:"%F %d, %Y"
	});
};
